# Site Contracts

> Every site has a contract. These contracts are enforced by the canon-manifest agent, domain stewards, and release gates.

## The 10 Shared Truths

1. **The root explains the whole.** `arif-fazil.com` is the entry point. Every other site links back to it.
2. **Organ sites deepen one domain.** GEOX = earth, WEALTH = capital, WELL = readiness. No crossover without explicit handoff.
3. **MCP connects machines.** `mcp.arif-fazil.com` is the transport layer. It is not a marketing site or an observatory.
4. **arifOS governs authority.** The kernel (port 8088) adjudicates. The Observatory (arifos.arif-fazil.com) proves. They are separate surfaces.
5. **A-FORGE executes only after approval.** `forge.arif-fazil.com` is the controlled execution surface. No self-authorizing builds.
6. **The Observatory proves current state.** `arifos.arif-fazil.com` emits signed snapshots. Every release must match.
7. **VAULT999 preserves receipts.** Immutable append-only. Never edited, never deleted, never "cleaned up."
8. **Arif remains final authority.** F13 SOVEREIGN. 888 decides irreversible. No agent self-grants authority.
9. **Unknown must remain unknown.** If an organ is down, the page says "Unknown" — not "healthy" and not "down" without evidence.
10. **Live state must never be hard-coded into page copy.** Tool counts, organ status, release versions — all come from Observatory or live probes, never from static HTML.

## Per-Site Contracts

### arif-fazil.com (Ψ SOUL)

- **Must:** Be the human anchor — identity, portfolio, genesis (`/000`), verification (`/999`).
- **Must:** Include Trinity navigation on every page.
- **Must:** Link to every organ site.
- **Must NOT:** Become an operations dashboard.
- **Must NOT:** Display live organ status as hard-coded HTML.
- **Must NOT:** Claim authority over organ domain content.

### arifos.arif-fazil.com (Ω MIND)

- **Must:** Serve the Observatory — signed snapshots, public-state.json, DID.
- **Must:** Document the 13 constitutional floors.
- **Must:** Serve `/wiki/` as the constitutional knowledge base.
- **Must NOT:** Become an execution surface.
- **Must NOT:** Accept mutations (read-only surface).

### aaa.arif-fazil.com (Δ BODY)

- **Must:** Be the operator cockpit — Arif and trusted operators only.
- **Must:** Display agent status, run logs, and approval queues.
- **Must NOT:** Become a public homepage.
- **Must NOT:** Expose operator PII or credentials.

### geox.arif-fazil.com (🌊 EARTH)

- **Must:** Display geological evidence, basin data, prospect evaluations.
- **Must:** Label all claims with epistemic tags (OBS/DER/INT/SPEC).
- **Must:** Include uncertainty bounds on all quantitative claims.
- **Must NOT:** Claim capital authority.
- **Must NOT:** Approve drilling decisions.

### wealth.arif-fazil.com (💰 CAPITAL)

- **Must:** Compute capital metrics (NPV, IRR, EMV, Kelly, etc.).
- **Must:** Declare all assumptions explicitly.
- **Must:** Include "WEALTH computes. arifOS judges. Arif decides." on every page.
- **Must NOT:** Become a trading oracle.
- **Must NOT:** Issue investment instructions.

### well.arif-fazil.com (🫀 READINESS)

- **Must:** Reflect human and machine readiness.
- **Must:** Label all assessments REFLECT_ONLY.
- **Must:** Preserve dignity (F6 MARUAH).
- **Must NOT:** Diagnose medical conditions.
- **Must NOT:** Expose private biological information.

### mcp.arif-fazil.com (🔌 GATEWAY)

- **Must:** Serve the MCP transport layer.
- **Must:** Provide `/mcp`, `/health`, `/.well-known/mcp/server.json`.
- **Must:** Maintain `llms.txt`.
- **Must NOT:** Become a marketing site.
- **Must NOT:** Duplicate Observatory content.

---

*DITEMPA BUKAN DIBERI — Forged, Not Given*
